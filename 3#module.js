// Common Js , every file is Module (by default)
// Modules - Encapsulated code (only share minimum) 


let ram = 'ram';
let kamal = 'Kamal';

const SayHi = (name) => {
    console.log(`Hey Mr/Mrs.  ${name} `);
}

SayHi('raman');
SayHi(ram);
SayHi(kamal);

